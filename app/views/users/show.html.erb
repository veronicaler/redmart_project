<p id="notice"><%= notice %></p>

<div class="row">
    <% if logged_in? %>
    <aside class="col-md-6">
        <section class="user_info">
            <%= render 'shared/user_info' %>

            <%= link_to 'Edit', edit_user_path(@user) %>
            |
            <%= link_to 'Back', root_path %>
        </section>
          <%= link_to image_tag("user.png", alt: "user",)%>
    </aside>
    <% end %>

    <div class="col-md-6">
        <h3>Your Reviews</h3>
        <%= render 'shared/feed' %>
    </div>
</div>
